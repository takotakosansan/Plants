
<h1>投稿一覧</h1>

<div class="container">
   <div class="row">
   <% @posts.each do |posts| %>
   <div class="col-md-4">
      <%= link_to post_path(posts.id) do %>
      <p><%= posts.name%></p>
      <% end %>
      <p><%= posts.description %></p>
     </div>
   <% end %>

   </div>
  </div>